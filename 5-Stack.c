#include "monty.h"
/**
 * f_stack - this is to print the top
 * @head: the head of the stack
 * @counter: this is the line_number
 * Return: No Return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
